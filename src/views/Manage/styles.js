import { StyleSheet } from "react-native";
export default styles = StyleSheet.create({
    announceCta: {
        height: 50,
        fontSize: 30,
        fontWeight: 'bold'
    },
    manageContainer: {
        paddingBottom: 64,
        paddingTop: 32
    },
    manageTitle: {
        // margin: 0,
        // padding: 100
    }
});
